set -g default-terminal "tmux-256color"
# true color + OSC 7 (cwd) forwarding to the outer terminal, for tmux-window-status's colored window titles
set -as terminal-features ',xterm*:RGB'
set -as terminal-features ',xterm*:osc7'
set -g default-shell $SHELL
if -b 'command -v reattach-to-user-namespace > /dev/null 2>&1' 'set -g default-command "reattach-to-user-namespace -l ${SHELL}"'

# with follow mode, exiting the last shell in a session switches to another session
# instead of detaching the client
set -g detach-on-destroy off

setw -g mode-keys vi

# vim-style window movement
bind-key h previous-window
bind-key l next-window

# prefix+p belongs to lemonaid (below); paste is prefix+P (and the default prefix+])
unbind p
bind P paste-buffer
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'V' send -X select-line
bind-key -T copy-mode-vi 'r' send -X rectangle-toggle
bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel "pbcopy"

set-option -g prefix C-x
unbind-key C-b
bind-key C-x send-prefix

#### COLOUR (Solarized 256)

# default statusbar colors
set-option -g status-style bg=colour236,fg=colour109

# window titles: tmux-window-status (~/tools/main) colors each by directory / process, so no fg here
set-window-option -g window-status-style bg=colour236
set-window-option -g window-status-current-style bg=colour238
set-window-option -g window-status-separator "│"

# session name, inverted, colored by name
set -g status-left '#(lemonaid-tmux-session-name "#{session_name}") '
set -g status-left-length 30
# Show Git working tree status
set -g status-right '#(gitmux "#{pane_current_path}" -cfg .gitmux.conf)'

## pane border
#set-option -g pane-border-fg colour235 #base02
#set-option -g pane-active-border-fg colour240 #base01
#
## message text
#set-option -g message-bg colour235 #base02
#set-option -g message-fg colour166 #orange
#
## pane number display
#set-option -g display-panes-active-colour colour33 #blue
#set-option -g display-panes-colour colour166 #orange
#
## clock
#set-window-option -g clock-mode-colour colour64 #green
#
## bell
#set-window-option -g window-status-bell-style fg=colour235,bg=colour160 #base02, red

# Window titles: `parent/child` with each segment colored deterministically, or the app
# running in the pane (claude, emacs, ...). pane_path comes from OSC 7, which oh-my-zsh
# already emits on every prompt (lib/termsupport.zsh), so no shell snippet is needed.
# Source: ~/tools/main/src/tools/tmux/window_status.py
setw -g window-status-format " #I:#(tmux-window-status '#{pane_path}' '#{pane_current_path}' '#{pane_current_command}' '#{pane_title}') "
setw -g window-status-current-format " #I:#(tmux-window-status '#{pane_path}' '#{pane_current_path}' '#{pane_current_command}' '#{pane_title}') "

#
# lemonaid: see ~/tools/lemonaid/docs/tmux.md
#

# prefix+p: ping-pong between where you are and where you came from (the inbox saves
# your location every time it switches you somewhere)
bind-key p run-shell ' \
    target=$(lemonaid tmux swap "#{session_name}" "#{pane_id}"); \
    if [ -n "$target" ]; then \
        tmux switch-client -t "$(echo "$target" | cut -d"|" -f2)"; \
    fi'

# prefix+a ("aid"): show / focus / hide the inbox pane (in follow mode: toggle focus only)
bind-key a run-shell 'lemonaid tmux scratch'
# prefix+A: move the inbox pane between the left edge (cards) and the top (columns)
bind-key A run-shell 'lemonaid tmux scratch --flip'
