[project]
name = "claude"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["tools"]

[dependency-groups]
dev = ["mypy>=1.19.1"]

[tool.uv.sources]
"tools" = { path = "../main", editable = true }
